#include<bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int t;
    cin>>t;

    while(t--){
        int n;
        cin>>n;

        vector<int> a(n), b(n);
        for(auto i:&a){
            cin>>i;
        }

        for(auto i:&b){
            cin>>i;
        }

        int g
        for(int )
        
        for(int i=0;i<n;i++){

        }
    }
}